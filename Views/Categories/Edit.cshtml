@model WebAppMVC.Domain.Models.Products.Category

@section title {
    @if (Model != null)
    {
        <h3>Editar Categoria</h3>
    }
}

@if (Model != null)
{
    <partial name="_Categories" model="@Model"/>
}