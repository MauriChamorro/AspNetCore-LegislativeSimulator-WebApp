@model WebAppMVC.Domain.Models.Category

@section title {
    @if (Model != null)
    {
        <h3>Editar Categoria</h3>
    }
}

@if (Model != null)
{
    <partial name="_Categories" model="@Model"/>
}