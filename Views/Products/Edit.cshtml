@model ProductViewModel

@section title {
    @if (Model != null)
    {
        <h3>Editar Producto: @Model.Product?.Name</h3>
    }
}

@if (Model != null)
{
    <partial name="_Products" model="@Model"/>
}